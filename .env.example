# Copy to .env and adjust for your .NET API
VITE_API_BASE_URL=http://localhost:5183/api

# true = login/register work without backend (UI development only)
# false = use real /auth/login and /auth/register
VITE_DEV_MOCK_AUTH=false
